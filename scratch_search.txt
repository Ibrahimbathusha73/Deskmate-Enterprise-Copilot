[{'id': 'github_issue_47831_0',
  'score': -7.651711463928223,
  'text': 'Issue #47831: `Qwen3_5GatedDeltaNet` / `Qwen3_5MoeGatedDeltaNet` '
          'initialize `A_log` to `-inf` on some heads (bf16 models) State: '
          'open ### System Info - `transformers` version: 5.14.0 - Platform: '
          'Linux (glibc 2.34) - Python version: 3.12.5 - PyTorch version '
          '(GPU?): 2.13.0+cu126 (yes, 8× NVIDIA H200) - Model: Qwen3.5-MoE '
          'hybrid (40 layers = 30 GatedDeltaNet + 10 full attention, 256 '
          'experts), trained from scratch in bf16 Output of `transformers env` '
          'is consistent with the above. ### Who can help? Text/hybrid '
          '(linear-attention) models — Qwen3.5 GatedDeltaNet (@ArthurZucker '
          '@Cyrilvallez @vasqu) ### Information - [x] The official example '
          'scripts - [ ] My own modified scripts ### Tasks - [x] An officially '
          'supported task in the `examples` folder (such as GLUE/SQuAD, ...) - '
          '[ ] My own task or dataset (give details below) ### Reproduction '
          'When a Qwen3.5 (`qwen3_5`) or Qwen3.5-MoE (`qwen3_5_moe`) model is '
          'instantiated in bfloat16 (the normal case for `from_config(..., '
          'dtype=torch.bfloat16)` or `from_pretrained(..., '
          'dtype=torch.bfloat16)` on a fresh config), a fraction of the '
          'GatedDeltaNet heads get `A_log = -inf`, i.e. `A = exp(A_log) = 0`. '
          'The cause is the decay-parameter initialization in '
          '`Qwen3_5GatedDeltaNet.__init__` / '
          '`Qwen3_5MoeGatedDeltaNet.__init__`: ```python A = '
          'torch.empty(self.num_v_heads).uniform_(0, 16) # inherits model '
          'dtype (bf16) self.A_log = nn.Parameter(torch.log(A)) ``` '
          "`torch.empty(self.num_v_heads)` inherits the module's default "
          'dtype. Under a bf16 model that is **bfloat16**, where `uniform_(0, '
          '16)` rounds a draw down to exactly `0.0` with probability ≈ 0.4% '
          "per head. `torch.log(0) = -inf`, so that head's decay `A = "
          'exp(A_log) = 0`. Consequences for the affected head: - `g = -A * '
          'softplus(a + dt_bias) = 0` -> the gated-delta recurrence **never '
          'decays/forgets** for that head (its state accumulates without '
          'bound). - `dg/dA_log = -A * softplus(...) = 0` -> the gradient '
          'w.r.t. `A_log` is exactly 0, so the head is **permanently frozen at '
          '`-inf`** and can never recover. Because the probability compounds '
          'over `num_value_heads × num_gdn_layers`, small models are usually '
          'spared while large hybrids are reliably affected. On our 35B-A3B '
          'config (32 value heads × 30 GDN layers ≈ 960 draws) we observe ~3–6 '
          'dead heads at every initialization; an 8×8 tiny config shows none. '
          '## Reproduction ```python import torch torch.manual_seed(0) '
          'num_value_heads, num_gdn_layers = 32, 30 def count_dead(dtype): '
          'dead = 0 for _ in range(num_gdn_layers): A = '
          'torch.empty(num_value_heads, dtype=dtype).uniform_(0, 16) # current '
          'code A_log = torch.log(A.float()) if dtype == torch.float16 else '
          'torch.log(A) dead += int((~torch.isfinite(A_log)).sum()) return '
          'dead print("bfloat16 dead heads:", count_dead(torch.bfloat16)) # '
          'e.g. 6 print("float32 dead heads:", count_dead(torch.float32)) # 0 '
          '``` Or directly on a model: ```python import torch from '
          'transformers import AutoModelForCausalLM, AutoConfig config = '
          'AutoConfig.from_pretrained("<a qwen3_5_moe hybrid config>") model = '
          'AutoModelForCausalLM.from_config(config, dtype=torch.bfloat16) for '
          'name, p in model.named_parameters(): if name.endswith("A_log") and '
          'not torch.isfinite(p).all(): print(name, "->", '
          'int((~torch.isfinite(p)).sum()), "dead head(s)") ``` ### Expected '
          'behavior `A_log` should be finite for every head at initialization, '
          'regardless of the model dtype. No head should start with a zero '
          '(never-forgetting) decay gate. ## Suggested fix Sample `A` in '
          'float32 before taking the log, matching the reference '
          'implementations already in the repo. This is a 1-line change in '
          'each of `Qwen3_5GatedDeltaNet.__init__` and '
          '`Qwen3_5MoeGatedDeltaNet.__init__`: ```diff - A = '
          'torch.empty(self.num_v_heads).uniform_(0, 16) + A = '
          'torch.empty(self.num_v_heads, dtype=torch.float32).uniform_(0,'},
 {'id': 'github_issue_47806_0',
  'score': -8.410634994506836,
  'text': 'Issue #47806: [serge] Fix 1 integration test for model '
          '`qwen3_vl_moe` failing with `output_mismatch` (list output differs '
          '(1)) State: open <!-- ci-dashboard-badge:start --> '
          '[![CI](https://transformers-ci.lor-e.huggingface.cool/badge/pr?pr=47806)](https://transformers-ci.lor-e.huggingface.cool/d/pytest-observability-by-pr/pytest-observability-branch?var-pr=47806) '
          '<!-- ci-dashboard-badge:end --> ## Original CI failure - Failure '
          'group: `1/1: 1 integration test for model `qwen3_vl_moe` failing '
          'with `output_mismatch` (list output differs (1))` - '
          '`tests/models/qwen3_vl_moe/test_modeling_qwen3_vl_moe.py::Qwen3VLMoeIntegrationTest::test_small_model_integration_test_batch` '
          '[single-gpu] (output_mismatch, seen 7/7) <!-- '
          'serge-task:integration-failure-triage:sha256:97018fe2b86e4f108dfedbfa0b4105bf7c5c633cd2e2876c07803187d8b6d300 '
          '--> Relates to #47804 '
          '`Qwen3VLMoeIntegrationTest::test_small_model_integration_test_batch` '
          "was failing because the generated caption for the Pallas's cat "
          "image changed wording. The model now outputs: > This is a Pallas's "
          "cat, also known as the manul. It's a wild cat species native to the "
          'grasslands and steppes instead of the previously expected "small '
          'wild cat native to the grasslands and montane regions". This is a '
          'stale expected-value issue, not a regression in model code, so '
          "update the test's `EXPECTED_DECODED_TEXT` to match the current "
          'model output. --- ### ✅ Verified on GPU serge ran the targeted '
          '`@slow` test(s) on a GPU runner and opened this PR only after they '
          'passed with this patch. Each targeted test was run 5× on both the '
          'pre-patch and patched trees to rule out flakiness — the result held '
          'on every run. - **Passes with this patch:** '
          'https://github.com/huggingface/transformers/actions/runs/31055835057 '
          '- **Failed before the fix (base commit):** '
          'https://github.com/huggingface/transformers/actions/runs/31052821646 '
          '--- _This change was produced automatically by serge from a CI '
          'failure report. The patch was generated by an LLM and applied by '
          'serge; review before merging._ _serge `v0.1.0` · model: '
          '`moonshotai/Kimi-K2.7-Code` · 18 LLM turns · 17 tool calls · 17.7s '
          '· 319681 in / 2757 out tokens_'},
 {'id': 'github_issue_47887_0',
  'score': -8.665861129760742,
  'text': 'Issue #47887: fix(qwen3_5,qwen3_next): sample GatedDeltaNet A in '
          'float32 to avoid -… State: closed <!-- ci-dashboard-badge:start --> '
          '[![CI](https://transformers-ci.lor-e.huggingface.cool/badge/pr?pr=47887)](https://transformers-ci.lor-e.huggingface.cool/d/pytest-observability-by-pr/pytest-observability-branch?var-pr=47887) '
          '<!-- ci-dashboard-badge:end --> # fix(qwen3_5,qwen3_next): sample '
          'GatedDeltaNet A in float32 to avoid -inf A_log under bf16 Under '
          '`bfloat16` `torch.empty(...).uniform_(0,16)` can round to `0.0` so '
          "`log(A)` becomes `-inf`, freezing that head's decay at `0`. Sample "
          '`A` in `float32` before `log` in `__init__` and `_init_weights` for '
          '`Qwen3_5`, `Qwen3_5-MoE` and `Qwen3-Next` (modeling + modular). '
          'Matches `Mamba2`. Fixes #47831 # What does this PR do? Fixes #47831 '
          'Under `bfloat16`, the GatedDeltaNet decay parameter `A_log` is '
          'initialized from `A = torch.empty(self.num_v_heads).uniform_(0,16)` '
          "which inherits the model's `dtype`. In `bfloat16` that "
          '`uniform_(0,16)` rounds to exactly `0.0` with ~0.4% probability per '
          'head. `torch.log(0) = -inf` so `A = exp(A_log) = 0` → `g = '
          '-A*softplus(a+dt_bias) = 0` (head never forgets) and `dg/dA_log = '
          '0` (gradient frozen, can never recover). On the 35B-A3B hybrid '
          'config (32 value heads × 30 GDN layers ≈960 draws) this is reliably '
          '3–6 dead heads at every initialization. **Change:** Sample `A` in '
          '`float32` before `log`, matching the reference already used in '
          '`Mamba2Mixer` and `modeling_mamba2.py`: - `__init__`: '
          '`torch.empty(self.num_v_heads, dtype=torch.float32).uniform_(0,16)` '
          'in `Qwen3_5GatedDeltaNet`, `Qwen3_5MoeGatedDeltaNet`, '
          '`Qwen3NextGatedDeltaNet` (modeling + modular) - `_init_weights`: '
          '`torch.empty(module.num_v_heads, dtype=torch.float32, '
          'device=module.A_log.device).uniform_(0,16).log_()` in all 6 files '
          '(`modeling` + `modular` for `qwen3_5`, `qwen3_5_moe`, `qwen3_next`) '
          'This keeps `A_log` finite for every head regardless of model '
          '`dtype`, under both construction and `_init_weights` '
          're-initialization. Forward already uses `A_log.float().exp()`, so '
          "no behavior change beyond the init distribution's precision (which "
          'is intended). **Dependencies:** None. **Docs:** N/A (init-only, no '
          'public API). **Repro** (from issue, seed 0): ```python import torch '
          'torch.manual_seed(0) num_v, layers = 32, 30 def count_dead(dtype, '
          'float32_fix=False): dead=0 for _ in range(layers): A = '
          'torch.empty(num_v, dtype=torch.float32 if float32_fix else '
          'dtype).uniform_(0,16) dead += '
          'int((~torch.isfinite(torch.log(A))).sum()) return dead '
          'print(count_dead(torch.bfloat16, False)) # 6 '
          'print(count_dead(torch.bfloat16, True)) # 0 ``` **Duplicate-work '
          'check:** Prior PR #47842 addressed the same issue but is **closed, '
          'not merged** (no longer open). Verified via `gh pr list --repo '
          'huggingface/transformers --state open --search "47831 in:body"` → 0 '
          'open. This PR is a successor with the same 50+/10- dtype fix, '
          're-applied on top of current `main` (`c7e57f7`) and ready for '
          'review. Issue #47831 author (@Nkluge-correa) invited a PR in the '
          'issue body. ## Code Agent Policy The Transformers repo is currently '
          'being overwhelmed by a large number of PRs and issue comments '
          'written by code agents. These often are low-quality, or fix '
          'extremely minor issues that occur rarely or never in practice. As a '
          "result, we're instituting a rule that **first-time contributors "
          "should not use code agents to submit PRs or issues**. We'd also ask "
          'autonomous "OpenClaw"-like agents not to open any PRs or issues. '
          'Issues/PRs from first-time contributors that violate this rule will '
          'probably just be closed without review, and we might block you, '
          'especially if you open more than one or appear to be deliberately '
          'ignoring this. We especially do not want new contributors to jump '
          'in on random issues to contribute an agent-written fix. This '
          'creates lots of noise for reviewers and other users and will almost '
          'certainly get you blocked. For more information, please'},
 {'id': 'github_issue_47872_0',
  'score': -8.689677238464355,
  'text': 'Issue #47872: Fix Qwen2.5-Omni / Qwen3-Omni-MoE generation with a '
          'compilable cache State: open <!-- ci-dashboard-badge:start --> '
          '[![CI](https://transformers-ci.lor-e.huggingface.cool/badge/pr?pr=47872)](https://transformers-ci.lor-e.huggingface.cool/d/pytest-observability-by-pr/pytest-observability-branch?var-pr=47872) '
          '<!-- ci-dashboard-badge:end --> ## What Override '
          '`create_masks_for_generate` on '
          '`Qwen2_5OmniPreTrainedModelForConditionalGeneration` (and, through '
          'modular, on `Qwen3OmniMoePreTrainedModelForConditionalGeneration`) '
          'so that the raw 2D attention mask is forwarded to the model instead '
          'of the pre-built 4D masks. ## Reproduction ```python import torch '
          'from transformers import AutoProcessor, '
          'Qwen2_5OmniForConditionalGeneration model_id = '
          '"Qwen/Qwen2.5-Omni-7B" processor = '
          'AutoProcessor.from_pretrained(model_id) model = '
          'Qwen2_5OmniForConditionalGeneration.from_pretrained( model_id, '
          'dtype=torch.bfloat16, device_map="auto", ).eval() inputs = '
          'processor(text="Describe this.", '
          'return_tensors="pt").to(model.device) # `max_new_tokens` alone is '
          'shadowed by the `thinker_max_new_tokens` argument of the omni '
          '`generate` outputs = model.generate( **inputs, '
          'thinker_max_new_tokens=32, cache_implementation="static", '
          'generation_mode="text" ) print(processor.batch_decode(outputs, '
          'skip_special_tokens=True)[0]) ``` ``` File '
          '"src/transformers/models/qwen2_5_omni/modeling_qwen2_5_omni.py", '
          'line 1974, in forward delta0 = (1 - '
          'attention_mask).sum(dim=-1).unsqueeze(1) TypeError: unsupported '
          "operand type(s) for -: 'int' and 'dict' ``` ## Why When the cache "
          'is compilable (`cache_implementation="static"`, '
          '`"offloaded_static"`, ...), `generate` calls '
          '`create_masks_for_generate` and replaces `attention_mask` with a '
          'dict of 4D masks keyed by layer type. Unlike most models, Qwen '
          "omni's `prepare_inputs_for_generation` sets `position_ids = None`, "
          'so `forward` recomputes the multimodal rope positions itself and '
          'needs the 2D padding mask to do so — it now receives a dict and '
          'crashes. Qwen2.5-VL is unaffected because it derives mrope from '
          '`mm_token_type_ids` rather than from the padding mask. **Before:** '
          '`generate` builds the 4D masks → `forward` gets a dict → '
          '`TypeError`. **After:** `create_masks_for_generate` returns the 2D '
          'mask (the same deferral the default implementation in '
          '`masking_utils` performs when no mask function is registered), and '
          '`Qwen2_5OmniThinkerTextModel.forward` builds the 4D masks through '
          'its existing `if not isinstance(causal_mask_mapping := '
          'attention_mask, dict)` branch. Nothing is lost: these models set '
          '`_can_compile_fullgraph = False`, so the masks could not be baked '
          'into a full graph anyway. ## Testing - Repro script above now '
          'generates normally with `cache_implementation="static"`. - Static '
          'vs. dynamic cache produce identical tokens, both on a tiny '
          'random-weight `Qwen2_5OmniThinker` and on `Qwen2.5-Omni-7B` with '
          'left padding. - '
          '`tests/models/qwen2_5_omni/test_modeling_qwen2_5_omni.py` and '
          '`tests/models/qwen2_5_vl/test_modeling_qwen2_5_vl.py -k '
          'test_generate_with_static_cache` pass (the only failure, '
          '`test_resize_embeddings_untied_with_deepspeed`, also fails on '
          '`main`).'},
 {'id': 'github_issue_47833_0',
  'score': -8.909515380859375,
  'text': 'Issue #47833: Use tiny Hub checkpoint in Qwen3ASR processor test '
          'State: closed <!-- ci-dashboard-badge:start --> '
          '[![CI](https://transformers-ci.lor-e.huggingface.cool/badge/pr?pr=47833)](https://transformers-ci.lor-e.huggingface.cool/d/pytest-observability-by-pr/pytest-observability-branch?var-pr=47833) '
          '<!-- ci-dashboard-badge:end --> ## Summary - Adds '
          '`hf-internal-testing/tiny-processor-qwen3_asr` (BPE 151k→500 '
          'tokens, built from `Qwen/Qwen3-ASR-0.6B-hf`) as the tiny checkpoint '
          'for `Qwen3ASRProcessorTest` - Removes the custom `setUpClass` / '
          '`tearDownClass` / `get_tokenizer` / `get_feature_extractor` / '
          '`get_processor` overrides — the `ProcessorTesterMixin` defaults '
          'handle all of this - Custom tests now load from `self.tmpdirname` '
          '(populated once in setup) instead of re-downloading from the Hub on '
          'every test call ## Performance > **Note**: timings are per-test '
          'body only and do not include `setUp`/`tearDown`. **Before** (full '
          '`Qwen/Qwen3-ASR-0.6B-hf`, 65 tests): | Time | Status | Test | '
          '|---:|---|---| | 6.79s | PASSED | test_chat_template_save_loading | '
          '| 5.58s | PASSED | test_save_load_pretrained_default | | 4.44s | '
          'PASSED | test_processor_from_and_save_pretrained_as_nested_dict | | '
          '3.97s | PASSED | test_can_load_various_tokenizers | | 3.68s | '
          'PASSED | test_apply_transcription_request_mixed_batch | | 3.33s | '
          'PASSED | test_decode_formats | | 3.24s | PASSED | '
          'test_processor_from_and_save_pretrained | | 2.91s | PASSED | '
          'test_apply_transcription_request_with_language | | 2.72s | PASSED | '
          'test_apply_transcription_request_with_prompt | | 2.67s | PASSED | '
          'test_processor_from_pretrained_vs_from_components | | 2.57s | '
          'PASSED | test_chat_template | | 2.47s | SKIPPED | '
          'test_chat_template_audio_from_video | | ... | | | | **76.80s** | | '
          '**Total** | **After** (tiny Hub checkpoint, 33 tests): | Time | '
          'Status | Test | |---:|---|---| | 0.54s | PASSED | '
          'test_apply_transcription_request_mixed_batch | | 0.46s | PASSED | '
          'test_apply_transcription_request_with_language | | 0.45s | PASSED | '
          'test_apply_transcription_request_with_prompt | | 0.06s | SKIPPED | '
          'test_feature_extractor_defaults | | 0.03s | PASSED | '
          'test_tokenizer_decode_defaults | | 0.02s | PASSED | '
          'test_processor_from_and_save_pretrained_as_nested_dict | | 0.02s | '
          'PASSED | test_processor_from_and_save_pretrained | | 0.01s | PASSED '
          '| test_save_load_pretrained_additional_features | | 0.01s | PASSED '
          '| test_processor_to_json_string | | 0.01s | PASSED | '
          'test_chat_template_saving_rejects_path_traversal | | 0.01s | PASSED '
          '| test_chat_template | | ... | | | | **1.71s** | | **Total** | '
          '**~45× faster** (76.80s → 1.71s). Skipped tests also drop from '
          '~1–2s to ~0s each, since `setUpClass` no longer downloads the full '
          '151k-token checkpoint. Continues the work started in #47213.'}]
